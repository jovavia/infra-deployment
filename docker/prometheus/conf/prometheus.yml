global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: postgres
    static_configs:
      - targets:
          - postgres-exporter:9187

  - job_name: pgbouncer
    static_configs:
      - targets:
          - pgbouncer-exporter:9127